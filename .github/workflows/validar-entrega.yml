name: Validar Entrega

on:
  pull_request:
    branches: [main]
    paths:
      - 'ENTREGA.md'

jobs:
  validar-entrega:
    uses: profBruno-UFC-Qx/kit-projeto-final/.github/workflows/validar-entrega.yml@main
